# Enable sendfile for faster local file serving
<IfModule mod_sendfile.c>
    SendFile on
</IfModule>

# Increase timeouts for large local transfers
<IfModule mod_core.c>
    KeepAliveTimeout 30
    Timeout 300
</IfModule>

# Ensure proper MIME types for video formats
<IfModule mod_mime.c>
    AddType video/mp4 .mp4 .m4v
    AddType video/webm .webm
    AddType video/ogg .ogv
    AddType video/x-matroska .mkv
</IfModule>

# Add browser caching controls for media files
<IfModule mod_headers.c>
    <FilesMatch "\.(mp4|m4v|webm|mkv|ogv)$">
        Header set Cache-Control "public, max-age=3600"
    </FilesMatch>
</IfModule>

# DLNA compliance headers for MP4 files (Sony Bravia compatible)
<IfModule mod_headers.c>
    <FilesMatch "\.mp4$">
        Header set Content-Type "video/mp4; DLNA.ORG_PN=AVC_MP4_HP_HD_24p"
        Header set contentFeatures.dlna.org "DLNA.ORG_PN=AVC_MP4_HP_HD_24p;DLNA.ORG_OP=03;DLNA.ORG_CI=0;DLNA.ORG_FLAGS=8d700000000000000000000000000000"
        Header set transferMode.dlna.org "Streaming"
        Header set realTimeInfo.dlna.org "DLNA.ORG_TLAG=*"
        Header set Accept-Ranges "bytes"
        Header set Connection "close"
    </FilesMatch>
</IfModule>

# Disable content negotiation for video files
<IfModule mod_negotiation.c>
    Options -MultiViews
</IfModule>
